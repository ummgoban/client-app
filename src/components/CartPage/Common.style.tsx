import styled from "@emotion/native"

const HeaderText = styled.Text`
  font-size: 16px;
  font-family: pretendard;
  font-weight: 600;
`
const BodyText = styled.Text`
  font-size: 14px;
  font-family: pretendard;
  font-weight: 400;
`
const S = {HeaderText, BodyText}

export default S