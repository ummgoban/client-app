import styled from '@emotion/native';

const S = {
  Container: styled.View`
    display: flex;
    flex-direction: row;
    padding: 8px;
    justify-content: center;
    align-items: center;
  `,
};

export default S;
