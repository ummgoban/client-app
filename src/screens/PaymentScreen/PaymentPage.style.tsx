import styled from '@emotion/native';

const PaymentPage = styled.View`
  flex: 1;

  background-color: white;
`;

const ScrollView = styled.ScrollView``;

const S = {PaymentPage, ScrollView};

export default S;
